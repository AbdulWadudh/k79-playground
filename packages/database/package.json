{
  "name": "database",
  "version": "1.0.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "dependencies": {
    "@prisma/client": "^5.5.2"
  },
  "devDependencies": {
    "prisma": "^5.5.2"
  },
  "scripts": {
    "db:push": "bunx prisma db push",
    "db:pull": "bunx prisma db pull",
    "db:generate": "bunx prisma db generate"
  }
}
